{
  "name": "formations",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "predeploy": "rm -rf dist && parcel build ./src/*.html --public-url https://a-xmd.github.io/player-rater",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.5.0",
    "gh-pages": "^3.2.3",
    "parcel": "^2.5.0",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
